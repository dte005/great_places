import 'package:flutter/material.dart';

class PlacesDetail extends StatelessWidget {
  const PlacesDetail({super.key});
  @override
  Widget build(BuildContext context) {
    throw Center(child: Text("Detalhe do place"));
  }
}
